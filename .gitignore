# Node
node_modules/
npm-debug.log*
package-lock.json

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
ENV/

# Google API & Secrets
config/*.json
!config/.env.example
config/token.json
config/.env
config/.env.threads

# Claude Code — skill 정의(.claude/skills/)는 commit, 사용자별 local 만 제외
.claude/settings.local.json
.claude/scratch/
.claude/scheduled_tasks.lock
.mcp.json
CLAUDE.md

# Logs
logs/*.log
logs/*.json

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Temporary
*.tmp
*.bak
.temp/

# Generated docs (during conversion)
# 자동 변환된 문서는 제외하되, 프로젝트 문서는 포함
docs/architecture/**/*.md
docs/development/**/*.md
docs/operations/**/*.md
docs/products/**/*.md
docs/_uncategorized/**/*.md
!docs/**/.gitkeep

# 프로젝트 문서는 커밋 (docs/ 루트)
!docs/*.md

# Images (downloaded from Threads, etc.)
docs/images/

bak
dorray-docs/

*__human.org
.direnv
/**/samples
!/samples/
!/samples/org-to-odt/
3rd

.agent-shell/
.beads/.br_history

tmp

# 프로젝트별 내보내기 스크립트 (계정/문서ID 포함)
scripts/export_kiat_*.sh
output

office

# scanpdf/ 는 별도 private git 리포 (저작권 스캔 PDF=LFS + 전사 작업물).
# memex-kb(공개)에서는 전부 무시한다. 데이터/작업은 scanpdf 리포에서 추적하고,
# 변환 도구만 여기 scanpdf2org/ 에 둔다. 두 리포를 나란히 클론해 쓴다.
scanpdf/

# OCR 멀티-엔진 평가 산출물 (루트 thin-client 출력 — 원자료는 scanpdf 리포에서 추적)
deepseek-out/
paddleocr-out/
ppstructure-out/
glm-out/
vltool-out/

# ROSSE syndicate 묶음 산출물 (생성물 — 입력 md만 추적)
out/syndicate/

# Anthropic 논문→Org 산출물 (원문 저작권=Anthropic → 로직만 추적, 산출물·에셋 제외)
out/anthropic-paper/

# LaTeX build artifacts
*.aux
*.bbl
*.blg
*.fdb_latexmk
*.fls
*.log
*.out
*.synctex.gz
# Keep .pdf and .tex for reproducibility
